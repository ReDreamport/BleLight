<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="5dp"
    android:paddingEnd="10dp"
    android:paddingStart="10dp"
    android:paddingTop="5dp" >

    <TextView
        android:id="@+id/device_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="#000"
        android:textColor="#FFF"
        android:textSize="20sp" >
    </TextView>
    
    <TextView
        android:id="@+id/device_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:background="#000"
        android:textColor="#FFF"
        android:textSize="14sp" />

    <ToggleButton
        android:id="@+id/change_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_below="@id/device_status"
        android:onClick="changeStatus" />

    <Button
        android:id="@+id/reconnect_device"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toStartOf="@id/change_status"
        android:layout_alignTop="@id/change_status"
        android:onClick="reconnect"
        android:text="@string/reconnect_device"
        android:textSize="15sp" />

    <Button
        android:id="@+id/device_settings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toStartOf="@id/reconnect_device"
        android:layout_alignTop="@id/change_status"
        android:onClick="deviceSettings"
        android:text="@string/device_details"
        android:textSize="15sp" />

</RelativeLayout>